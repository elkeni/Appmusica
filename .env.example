# ==========================================
# CLOUDTUNE - Environment Variables
# ==========================================
# PHASE 3: Comprehensive API configuration guide
# Copy this file to .env and fill in your actual values

# ==========================================
# YOUTUBE DATA API v3 (REQUIRED for audio streaming)
# ==========================================
# Get your API key from: https://console.cloud.google.com/
# 
# Steps:
# 1. Create a new project in Google Cloud Console
# 2. Enable "YouTube Data API v3"
# 3. Go to Credentials → Create Credentials → API Key
# 4. Copy the API key here
# 
# Quota: 10,000 points/day (100 points per search)
# With cache optimization: ~500 unique songs/day
# Without API key: Falls back to Invidious/Piped (unlimited, slower)
#
REACT_APP_YOUTUBE_API_KEY=

# Optional: Custom YouTube API base URL (leave empty for default)
REACT_APP_YOUTUBE_BASE_URL=https://www.googleapis.com/youtube/v3

# ==========================================
# DEEZER API (For rich metadata & artwork)
# ==========================================
# Deezer API is FREE and does NOT require an API key
# Leave empty - it works out of the box
#
REACT_APP_DEEZER_API_KEY=
REACT_APP_DEEZER_BASE_URL=https://api.deezer.com

# ==========================================
# ITUNES API (For high-res artwork)
# ==========================================
# iTunes API is FREE and does NOT require an API key
# Leave empty - it works out of the box
#
REACT_APP_ITUNES_BASE_URL=https://itunes.apple.com

# ==========================================
# SPOTIFY API (OPTIONAL - Removed from codebase)
# ==========================================
# NOTE: Spotify integration has been removed
# These variables are kept for backwards compatibility
# You can safely ignore them
#
REACT_APP_SPOTIFY_CLIENT_ID=
REACT_APP_SPOTIFY_CLIENT_SECRET=
REACT_APP_SPOTIFY_BASE_URL=https://api.spotify.com/v1

# ==========================================
# CORS PROXY (Optional)
# ==========================================
# Only needed if you encounter CORS issues
# Leave empty unless you have your own CORS proxy
#
REACT_APP_CORS_PROXY=

# ==========================================
# QUICK START CHECKLIST
# ==========================================
# 
# MINIMUM REQUIREMENTS (App will work with fallback):
# ✓ None! App works without any API keys using Invidious/Piped
#
# RECOMMENDED SETUP (Best performance):
# 1. [REQUIRED] Get YouTube API key (see instructions above)
# 2. [DONE] Deezer API works automatically (no key needed)
# 3. [DONE] iTunes API works automatically (no key needed)
#
# EXPECTED PERFORMANCE:
# - With YouTube API: ~500 unique songs/day, instant search
# - Without API key: Unlimited songs, 2-3 second delay per new song
# - Cached songs: Always instant (0 quota cost)
#
# ==========================================
# TROUBLESHOOTING
# ==========================================
#
# Error: "403 Forbidden" from YouTube
# → Solution: Generate a NEW API key (old one quota exceeded)
# → Temporary: Remove API key to use fallback mode
#
# Error: No sound when playing
# → Check: Volume is not muted in player controls
# → Check: Browser console for errors
# → Try: Different song (some videos may be region-locked)
#
# Error: Songs not loading
# → Check: Internet connection
# → Try: Clear localStorage (DevTools → Application → Clear Storage)
# → Check: Firewall not blocking YouTube/Deezer domains
#
# ==========================================
# MONITORING
# ==========================================
#
# Check quota usage:
# - Bottom-right corner shows "YouTube API" stats
# - Blue = Normal, Yellow = Low quota, Red = Critical, Purple = Fallback mode
#
# View cache:
# - DevTools → Application → LocalStorage → yt_video_cache_v2_*
#
# Check fallback status:
# - Console logs: "🔄 [Fallback] Using Invidious/Piped"
# - UI indicator: Purple "🔄 Fallback Mode" badge
#
# ==========================================
